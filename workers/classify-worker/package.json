{
  "name": "daleel-classify-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker hosting Daleel's commodity text/image classification on Workers AI (stateless labels + confidence; callers own thresholds and policy)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:qa": "wrangler deploy --env qa",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
