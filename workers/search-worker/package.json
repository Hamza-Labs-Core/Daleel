{
  "name": "daleel-search-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker hosting Daleel's external search calls (Phase A: SerpAPI + Google Places proxied verbatim, KV-cached, vendor keys held at the edge)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:qa": "wrangler deploy --env qa",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
